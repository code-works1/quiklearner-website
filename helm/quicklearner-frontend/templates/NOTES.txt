QuickLearner frontend has been deployed.

Namespace:
  {{ .Values.namespace.name }}

Release:
  {{ .Release.Name }}

Check pods:
  kubectl get pods -n {{ .Values.namespace.name }}

Check service:
  kubectl get svc -n {{ .Values.namespace.name }}

Check ingress:
  kubectl get ingress -n {{ .Values.namespace.name }}

Health endpoint:
  /api/health
